# P3 — Adversarial verification of the BLOCKER-graded findings

Checkout `/home/user/JTS` @ `2d571e6b8`. Read-only. Shipped config assumed:
**full profile Pi 5 + XVF3800 + Apple USB-C dongle DAC**; **streambox on Pi Zero 2 W**.

Bar applied (AGENTS.md): *Blocker = correctness / safety / secret / deploy / hardware /
audio-output / silent-failure that **can actually happen** on the shipped config.*
A defense-in-depth gap whose every in-repo caller is already correct is **not** a Blocker.

**Headline: 0 of 22 survive as Blockers.** 1 upgrade-worthy security bug (6c) and 2
items are borderline-Blocker (8, 10); the rest are Should-fix / Nit / Refuted.
Note also: **17 of the 22 were graded Should-fix by the source report itself** — the
Blocker escalation happened in aggregation, not in the tiles.

---

## Verdict table

| # | id | claim (compressed) | source grade | **verdict** | re-read (file:line) | reachable on shipped config? | existing pin / guard? | cleanest fix (≤1 line) | behavior test? |
|---|---|---|---|---|---|---|---|---|---|
| 1 | p1-T01 F3 | wake in acquire window + `MEASURE_PAUSE` → `begin_turn()` unbounded wait, silent, no cue | Should-fix | **DOWNGRADE→Should-fix** | `voice/output_gate.py:85-102`; `voice_daemon.py:2606,2647,2675-2686,3828,4857,5073` | Yes but **sub-ms window**: `_wake_late_cancelled("post_arb")` (`:3916-3942`) checks `_measurement_active`, and post_arb→`begin_turn`'s lock read has **no yield point**; only the `pause_admission`-yields-on-contended-lock ordering, or the `conn_paused` wait at `:3841`, opens it. Bounded at `MEASUREMENT_AUTOCLEAR_SEC=120.0` (`:573`), typically the sweep duration | The `post_arb` gate IS the designed guard. `tests/test_voice_output_gate.py:132` **pins the unbounded wait as intended**; daemon-level consequence unpinned | `_measurement_pause_detailed`: `if self._state is State.SESSION or self._acquiring: return "BUSY", None` (`:2647`) | Yes — pin `MEASURE_PAUSE`→`BUSY` while `_acquiring` |
| 2 | p1-T01 F4 | button + spend cap → `"CAP"`, no cue, no `log_event` | Should-fix | **CONFIRMED (Should-fix)** | `voice_daemon.py:4644-4645` vs siblings `:4631,:4638-4642,:4651-4657`; wake path cues at `:3836-3838` | Yes — WiiM Remote long-press → `accessories/registry.py:253` → `POST /session/start`. Refusal is **not fully silent**: `control/handlers/voice.py:95` maps CAP→**HTTP 503** with `{"result":"CAP"}`, and `bridge.py:220-247` deliberately does *not* retry a genuine CAP. But the household hears nothing | None. Both fixtures in `test_voice_daemon_manual_start_guard.py` stub `allowed=lambda: True` | `log_event(logger,"session.manual_refused",reason="spend_cap_reached")` + `await self._play_cue("spend_cap_reached")` before `:4645` | Yes — beside the existing `connection_paused` pin |
| 3 | p1-T01 F10 | escalation cue = bare `create_task`, no strong ref, no error handler | Should-fix | **DOWNGRADE→Nit** | `voice/_supervisor.py:286-302` | Marginal. The loop holds a ref while scheduled/awaiting; a raise still surfaces via asyncio's "exception was never retrieved". Only the *cue* is at risk, not the reconnect | Every other task in the tile is tracked; this is the lone exception | hold it on `OutageTracker` (assign to `self._cue_task`) | No |
| 4 | p1-T01 F11 | research-window cancel timeout drops wake, WARN only, no cue | Should-fix | **CONFIRMED (Should-fix)** | `voice_daemon.py:3466-3478`; `RESEARCH_CONFIRMATION_OPEN_CANCEL_TIMEOUT_SEC=20.0` at `:435` | Requires a wake landing while a research confirmation window is *opening* **and** the open outlasting 20 s. Narrow; needs the research pack enabled | None | `await self._play_cue(INTERNAL_ERROR_CUE_SLUG)` before the `return` at `:3478` | Yes — non-negotiable 6 |
| 5 | p1-T01 F5 | `_aec_reference_available` vs `contract_from_config` disagree → barge-in on un-cancelled audio | Should-fix | **REFUTED as Blocker → Should-fix (DRY only)** | `voice_daemon.py:501-513`; `voice/input_policy.py:87-131` | **No.** Shipped box has `aec_chip_aec_enabled` + `udp:` → first branch (`input_policy.py:96-108`, `echo_cancelled=True`) — **agrees** with `_aec_reference_available`. `udp:9876` also agrees. They diverge only for `custom_udp` (a `udp:<other port>` with chip AEC off) = an operator override the owner does not run | `tests/test_voice_daemon_barge_in.py:269-276` pins the *duplicate*, not the agreement | delete `_aec_reference_available`; `:1206` reads `contract_from_config(cfg).echo_cancelled` | No (behavior unchanged on shipped config) |
| 6a | p1-T02 | `_schedule_stop_retry` no attempt cap, rearms at 1 Hz forever | Blocker | **DOWNGRADE→Should-fix** | `wake_corpus/recording_backend.py:1504-1553` (`_stop_retry_attempts` only feeds the exponent, clamped at 8), `_retry_pending_stop:1556-1596` | Yes, but: daemon thread, ≤1 Hz (`STOP_RETRY_MAX_SEC=1.0`, `:132`), logs on powers of 2, and only while `_can_admit_current_locked`. Wake-corpus is a diagnostic subsystem — no audio-path effect | None | add `if self._stop_retry_attempts >= STOP_RETRY_MAX_ATTEMPTS: return` in `_schedule_stop_retry` | Yes — one pin on give-up |
| 6b | p1-T02 | `wake_events.sqlite3` no row retention | Blocker | **DOWNGRADE→Nit** | `wake_events.py:794-880` — a real 128 MiB **audio** retention sweep exists; `_scan_and_prune_blocking` deletes WAVs and leaves a sentinel in `audio_*_path`, so rows persist | Rows are ~1 KB; a year of heavy use is tens of MB on the SD card | Audio retention is the pin | add an oldest-first row prune to `_scan_and_prune_blocking` | No |
| 6c | p1-T02 | `get_travel_routes` declares `untrusted_output=True` but never arms the taint monitor | Blocker | **CONFIRMED — the one item worth Blocker-adjacent treatment** | `tools/travel_routes.py:114` (`make_travel_routes_tools(client)` takes **no** `monitor`), `:145` (`untrusted_output=True`); consumers `tools/__init__.py:139-168`; gate `tools/home_assistant.py:278-288` (`tainted = monitor is None or monitor.is_tainted()`); wiring `tools/packs.py:315-316` vs `:326,:359,:366` which all pass `monitor=d.untrusted_monitor` | Requires **both** the `travel-routes` pack (Google Routes key) **and** HA consequential actions enabled. Then third-party route text enters context **untainted**, so the next consequential HA action skips the confirmation gate. Exploitability is low (route text is largely user-seeded) but the declared contract is violated | None — no test pins declaration↔wiring | `packs.py:316`: `lambda d: make_travel_routes_tools(d.google_routes, monitor=d.untrusted_monitor)` (+ accept the kwarg and `monitor.mark()` in `get_travel_routes`) | **Yes** — a repo-wide pin: every tool with `untrusted_output=True` is built with a monitor |
| 7 | p1-T05 #1 | `set_active_config_raw` / `patch_config` do no `volume_limit` check; `/sound/` live-draft writes on every slider move (**non-negotiable 1**) | Blocker | **REFUTED as Blocker → Nit** | `camilla.py:891-928` (raw), `:1000-1039` (patch), `:143-162` (`_coerce_main_volume_db`, the fader clamp — intact); **both** emitter families gate at build time: `sound/camilla_yaml.py:456` and `active_speaker/camilla_yaml.py:414-417,2110,2280` → `camilla_config_contract.py:438-458` raises on `>0`; `DEFAULT_VOLUME_LIMIT_DB=0.0` at `:435` | **A `>0 dB` volume_limit is NOT constructible from `/sound/`.** `web/sound_setup.py:1391-1408` uploads `carrier.reemit(...)` output, which is emitter output. Every other `set_active_config_raw` caller passes emitter output or a read-back/parked YAML | `ensure_volume_limit_db` is the single-home threshold, consumed by both emitters | `set_active_config_raw`: assert the parsed `devices.volume_limit <= 0` before upload (belt-and-braces only) | No — behavior is already correct |
| 8 | p1-T08 + p2-S1 | `/system/restart/audio` `try-restart`s `jasper-usbsink-volume.service`, in **neither** `restart_broker` allowlist **nor** polkit → denial, 200 returned | Blocker | **CONFIRMED (Should-fix, borderline Blocker)** | Set difference re-derived at runtime: `local_source_audio_refresh_units()` (`local_sources/registry.py:170-177`) yields `jasper-usbsink-volume.service`, **absent** from `restart_broker.MANAGED_UNITS ∪ START_ONLY_UNITS`; polkit `deploy/polkit/49-jasper-control.rules:50-100` returns `NOT_HANDLED` → implicit `auth_admin_keep` → denied for a sessionless daemon; handler `control/handlers/system.py:459-471,504-517` uses **`Popen`** (no wait, no returncode) → **HTTP 200 regardless** | **Yes.** `jasper-control.service:115` is `User=jasper-control` (non-root), so polkit mediates. systemd calls `TryRestartUnit` **per unit**, so the allowlisted units restart and only this one is denied — a genuine partial, silent failure. PR **#4120 does NOT fix it** (it adds the unit to the *installer's* try-restart set, which runs as root; the polkit/broker gap is untouched) | `tests/test_polkit_jasper_control.py` pins broker↔polkit lockstep, but **nothing pins handler-unit-set ⊆ broker allowlist** | add `"jasper-usbsink-volume.service"` to `MANAGED_UNITS` in **both** `restart_broker.py` and the polkit rule | **Yes** — pin `local_source_audio_refresh_units() ⊆ MANAGED_UNITS ∪ START_ONLY_UNITS` |
| 8b | p2-S1 new | `systemctl reload avahi-daemon` granted to neither service user | Blocker | **DOWNGRADE→Nit** | `avahi_service.py:217-232` (`check=False`, `timeout=4`, stderr captured, **fail-soft** by design), `peering/avahi.py:126-138` (same); `avahi-daemon.service` absent from both polkit arrays | Denied, yes — but `/etc/avahi/services` **is** in `ReadWritePaths` (`jasper-control.service:160`), the file lands, and avahi's inotify picks it up. The docstring already calls the reload an optimisation | fail-soft is the guard | add `"avahi-daemon.service"` to `START_ONLY_UNITS` + polkit, or drop the reload | No |
| 8c | p2-S1 new | `POST /system/audio-quality` writes `/var/lib/jasper-asound` under `ProtectSystem=strict` with no `ReadWritePaths` | Blocker | **CONFIRMED (Should-fix) — endpoint is broken, but fails loudly** | `jasper-control.service:139,160` (`ReadWritePaths=/var/lib/jasper /var/lib/jasper-intsecrets /etc/avahi/services` — **`/var/lib/jasper-asound` absent**); `audio_quality.py:194-223` `subprocess.run(..., check=True)`; render target `deploy/bin/jasper-render-asound-conf:15,53-58` = `/var/lib/jasper-asound/asound.conf`; also root:root 0644 per `install.sh:1332,1342` | **Yes — the dashboard button can never succeed.** The subprocess inherits the unit's namespace → EROFS. **Not silent**: `handlers/system.py:347-354` catches `SubprocessError` → **HTTP 502** with the message; the old `asound.conf` survives (fail-closed) | 502 is the observability | add `/var/lib/jasper-asound` to `ReadWritePaths` (+ group ownership) | Yes — a hardening pin that the write target is in `ReadWritePaths` |
| 9 | p1-T18 | `secret_redaction` fails on `JASPER_WIFI_PSK=…` and `*_API_KEY=…` | Blocker | **DOWNGRADE→Should-fix (latent defect in an unreached defense)** | `secret_redaction.py:23-31` — the leading `\b` in `_KEY_VALUE_RE` cannot match after `_`. **Reproduced**: `JASPER_WIFI_PSK=hunter2secretvalue` → unchanged; `psk=…` → redacted | **Not reachable through any of the three consumers.** `_supervisor.py:99-107` (provider HTTP bodies), `doctor/_shared.py:77-84` (`str(exc)`), `doctor/voice.py:257-268` (child traceback line) all carry provider/exception text, never an env dump. And the three live provider keys are caught anyway by `_KEY_PREFIX_RE` (`sk-`/`AIza`/`xai-`, `:33`). Wi-Fi PSK code (`wifi_guardian_persistence.py`, `web/wifi_setup.py:44-49`) has its **own** scrubbers and never calls `redact_secrets` | **None — `redact_secrets` has zero tests repo-wide** (`grep redact_secrets tests/` = 0 hits). That absence is itself a finding on a secrets-adjacent module | `secret_redaction.py:25`: replace the leading `\b` with `(?<![A-Za-z0-9])` | **Yes** — one parametrized pin incl. the `_`-prefixed shapes |
| 10 | p1-T23 + T26-1/2 | `SKIP_INSTALL=1` skips identity + direction guards while `rsync --delete` runs (**non-negotiable 4**) | Blocker | **DOWNGRADE→Should-fix (borderline Blocker on the wording of NN-4)** | `scripts/deploy-to-pi.sh:590-592,602-675` — `resolve_speaker_hostname`, `warn_if_pi_host_on_gadget_network`, `preflight_sudo`, the identity guard and `preflight_deploy_direction` are all inside `if [[ "${SKIP_INSTALL:-}" != "1" ]]`; `rsync -az --delete` at `:688-693` runs unconditionally | **Blast radius is small.** rsync targets `${REMOTE_REPO_DIR}` (`~/jts`, the checkout) — **not `/opt/jasper`**, so a wrong Pi keeps running its installed code with zero service impact. What is destroyed: the wrong Pi's `~/jts` working tree (uncommitted work; `--delete`), and its `.env.local`/`CLAUDE.local.md` are overwritten with this checkout's. `captures/`, `wake-events/`, `logs/`, `.git` are excluded and therefore also `--delete`-protected. The direction guard is moot here (nothing is installed) | The identity guard is TOFU-recorded and already self-skips under `SUDO_INTERACTIVE=1` (`:625-629`) — so it is *already* a best-effort guard, which weakens the "bypass" framing | run the identity guard unconditionally: hoist it out of the `SKIP_INSTALL` branch. **Cost: one extra `ssh sudo cat`** (~100-300 ms) — cheap, but it makes `SKIP_INSTALL=1` require passwordless sudo, which is a real behavior change to the fast-iteration path | Yes — a shell pin that the guard runs on both branches |
| 11 | p1-T24 | streambox web unit runs `python -m jasper.web` as root with 4/14 hardening directives | Blocker → S1 re-graded | **CONFIRMED the re-grade (Should-fix)** | `deploy/jasper-web-streambox.service` (has `ProtectSystem=full`, `ProtectHome`, `PrivateTmp`, `NoNewPrivileges`) vs `deploy/jasper-web.service:126-127,143-144,157-167,195-202`. Missing and **uid-independent**: `CapabilityBoundingSet=`, `SystemCallFilter=`, `SystemCallArchitectures=`, `ProtectSystem=strict`, `ProtectKernelTunables/Modules/ControlGroups`, `RestrictNamespaces/SUIDSGID/AddressFamilies`, `LockPersonality` | Yes — the Zero 2 W streambox is a shipped target and this daemon is network-facing | **The `User=` deferral is legitimate**: documented removal condition ("streambox-hardware validation") and **pinned** by `tests/test_systemd_hardening.py:687-698`. So that half is REFUTED; the 11 uid-independent directives are the real gap and are pinned by nothing | copy the 11 uid-independent directives across (they need no user drop) | Yes — extend `test_systemd_hardening.py` to require the uid-independent set on both web units |
| 12 | p1-T14-4 | `commissioning_apply` "KNOWN DEFECT (#2202)" — apply cannot succeed on real hardware, still wired `tuning_owner="automatic"` | Blocker | **STATUS, not a verdict — owner-owned, already recorded** | `active_speaker/commissioning_apply.py:888-905` (the comment is accurate: `expected_graph` fingerprints the on-disk candidate, `observed_graph` is CamillaDSP's default-filled readback → cannot agree); caller `web/correction_crossover_backend.py:1487-1495` | The lane is blocked at `candidate_readback_mismatch` before the live-graph boundary — **fails closed** | **`docs/adr/0228…:88-101` row 9, STANDING**: *"the commissioning lane is being REPAIRED, not abandoned — the owner settled #2202 as fix"*; **"PR #3836 awaits the owner's re-ruling."** Both of row 9's premises are recorded as no longer holding at HEAD (ADR-0188 parked the relay lane, ADR-0197 deleted the sibling), and the ADR says re-deciding is **the owner's, not an agent's** | none — this is an owner re-ruling, not a code fix | n/a |
| 13 | p1-T13-1 | `crossover_v2/contracts.py:25` pulls numpy via `branch_chain` → the package's import-cost charter is false | Blocker | **CONFIRMED as fact → DOWNGRADE→Nit (a false docstring)** | `crossover_v2/__init__.py:5-13` claims *"Only `.contracts` is re-exported below — several of the rest pull `numpy` … so importers reach them by module path instead"*; `contracts.py:25` `from ..branch_chain import CrossoverSection`; `branch_chain.py:22` `import numpy as np`. **Measured**: `python3 -X importtime -c "import jasper.active_speaker.crossover_v2"` → `ModuleNotFoundError: numpy` in this env, i.e. the charter is falsified outright | crossover_v2 is tuning code = **full-profile Pi 5 only**, where numpy is installed. Cost is import time (~80-150 ms on slow hardware), no functional effect; it does **not** land on the Zero 2 W | none | fix the `__init__.py` docstring (a `TYPE_CHECKING` import won't work — `CrossoverSection` is used in a runtime `isinstance` at `contracts.py:320`) | No |
| 14 | p1-T11 | `runtime_integrity._read_status` unbounded `recv()` loop vs `route_latency`'s 1 MB cap | Blocker | **DOWNGRADE→Should-fix (duplication + latent DoS)** | `correction/runtime_integrity.py:96-118` (no byte cap; `settimeout` is per-`recv`, so a fast talker never trips it) vs `route_latency/status_socket.py:52,101,105` (`_RESPONSE_MAX_BYTES = 1_048_576`, enforced) | Peers are our own local Rust daemons (jasper-fanin, jasper-outputd) sending bounded JSON. Only a runaway local daemon triggers it — then it is an OOM vector on a 1 GB Pi | the capped reader exists and is the model | converge: `runtime_integrity` calls `route_latency.status_socket`'s reader | No |
| 15 | p1-T12-2 | `store_calibration` writes household state with bare `write_text` while importing `atomic_write_text` | Blocker | **CONFIRMED (Should-fix)** | `audio_measurement/calibration.py:36` imports `atomic_write_text`, uses it at `:1079`, but `:521` `raw_path.write_text(text)` and `:540` `metadata_path.write_text(json.dumps(...))` | Yes, but the write path is rare (importing a mic calibration file) and small. A power cut mid-write leaves a truncated `.json` (→ `load_calibration_record` raises at `:556`) or a truncated `.txt` curve that could parse with **fewer points** — the silent case | `file_sha256` is recorded in the metadata but nothing verifies it on read | `:521`/`:540` → `atomic_write_text(path, text, mode=0o600)` | No (atomicity isn't testable at this altitude) |
| 16a | p1-T09 | accessory reader tasks unsupervised; dead HID reader shows `restarts:0 last_error:null` in `/state` | Blocker | **CONFIRMED (Should-fix)** | `accessories/bridge.py:701` `asyncio.create_task(_read_device(...))`; `_read_device:536-542` docstring claims "Exits cleanly on unplug (OSError)" and only `:546` catches `OSError` on open; reap loop `:728-742` does `del active[p]` and **never** calls `.result()`/`.exception()`; the supervisor's health dict (`supervisor.py:62-96,109`) tracks **bridges**, not per-device readers | Yes. Worse than reported: the reap only runs after `await events.get()`, so a dead reader isn't even reaped until the next hot-plug, and `_maybe_start` only fires on udev `add` — a still-plugged accessory whose reader died is **permanently dead while `/state` reports the bridge healthy** | none. (The WiiM push-to-talk *mic* adapter is a separate, supervised `MIC_ADAPTERS` bridge, so wake/manual-session is not affected) | in the reap loop, log `active[p].exception()` and re-`_maybe_start(p)` | Yes — one pin that a reader raising a non-OSError is counted |
| 16b | p1-T09 | `jasper.identity` eagerly imports the peering state machine | Blocker | **DOWNGRADE→Should-fix (ADR-0226 import cost)** | `identity.py:42` `from .peering import config as peering_config` (needed only for `PEER_ID_FILE` at `:49` and `default_room()`); **measured** `python3 -X importtime -c "import jasper.identity"`: `jasper.peering.state` = **10.7 ms** of 60 ms total, on x86 — several× that on a Zero 2 W | Yes — `jasper.identity` is a broad reader (web renders, `control_advert`) and the streambox is a Zero 2 W | none | drop the eager `state` import from `jasper/peering/__init__.py` | No (measure, don't pin) |
| 17 | p1-T16-2 | `sound_setup._active_speaker_summed_test_payload` vs `web_commissioning.start_summed_test` are two divergent implementations | Blocker | **CONFIRMED user-visible divergence → Should-fix** | **Opposite audio defaults**: `web/sound_setup.py:4239` `wants_audio = bool(raw.get("audio"))` → **False**; `active_speaker/web_commissioning.py:2248` `bool(raw.get("audio", True))` → **True**. Different duration handling: `sound_setup.py:4237` `_summed_test_play_budget_seconds(raw.get("duration_ms"))` vs `web_commissioning.py:2267` `raw.get("duration_ms", 500)`. Different refusal shape: `web_commissioning.py:2215-2221` returns `{"status":"refused","reason":"measurement_in_progress"}` | **Yes, both ship on the full-profile Pi 5.** An identical POST with no `audio` key **plays a tone on `/correction/` and is silent on `/sound/`** | the audible path still goes through `arm_safe_playback_session` + the commissioning SPL stop, so no hearing risk | `/sound/` delegates to `web_commissioning.start_summed_test` (delete the sound_setup copy) | Yes — one pin on the shared default |
| 18a | p1-T20 | outputd ENODEV/EIO reach main as bare `Error:`; `main.rs:144` failed `STOPPING=1` notify masks the real error via `?` | Blocker | **REFUTED as Blocker → Nit** | `rust/jasper-outputd/src/main.rs:144` `notify_systemd("STOPPING=1")?;` — yes, it discards `result` and precedes the `runtime_error_exit_code` classification at `:145-152`. **But** `notify_systemd:1671-1683` returns `Ok(())` when `NOTIFY_SOCKET` is unset, and under `Type=notify` the socket exists for the unit's lifetime — a datagram send to it effectively never fails | Effectively unreachable | none | `let _ = notify_systemd("STOPPING=1");` | No |
| 18b | p1-T19-1 | unbounded xrun channel with a per-send heap alloc on the RT thread | Blocker | **CONFIRMED (Should-fix)** — note the report's file ref is wrong: it is **`rust/jasper-fanin/src/main.rs:201`**, not outputd | `let (xrun_tx, xrun_rx) = channel();` — `std::sync::mpsc::channel()` is unbounded; every `send` from the mixer heap-allocates a node, and the consumer thread `:203-215` does `fdatasync` to the SD card | Yes, and it self-reinforces: an xrun storm is exactly when the writer's fdatasync is slowest, so the queue grows on the RT thread precisely under load. 1 GB Pi | comment at `:198-200` acknowledges the design intent but not the unboundedness | `sync_channel(N)` + `try_send` at the mixer's send site (2 edits, not 1) | No |
| 18c | p1-T19-2 | `tts.rs prepare_period` allocs / `format!` on the audio thread during barge-in | Blocker | **REFUTED** | `rust/jasper-fanin/src/tts.rs:582-648` — grepped the whole body for `format!`/`String::`/`to_string`/`Vec::`/`vec![`/`Box::`/`clone()`/log macros: **zero hits**. The one `warn!` (`:656-666`) is in the *following* function, on the wire-width-mismatch path — a misconfiguration path, not barge-in | n/a | n/a | none | No |
| 18d | p1-T19-2 | `max_peak_dbfs` has no upper bound → positive assistant peak cap | Blocker | **DOWNGRADE→Nit** | `rust/jasper-fanin/src/config.rs:1131-1134` `env_f32("JASPER_OUTPUTD_ASSISTANT_MAX_PEAK_DBFS", loudness_defaults.max_peak_dbfs)` — no clamp; default `-3.0` (pinned at `:1370`) | Requires the owner to set the knob to a positive value by hand. It appears only in `.env.example` and `docs/audio-paths.md` — nothing sets it. Note it is also an **unrequested `JASPER_*` knob** per AGENTS.md Config | default `-3.0` is the guard in practice | clamp: `.min(0.0)` on the parsed value | No |
| 19 | p1-T21 | jasper-host-clock's `Dll` never ticked (`dll_err_frames` constant 0.0, `dll_locked` constant false, published to `/state` and pinned) | Blocker | **CONFIRMED as fact → DOWNGRADE→Should-fix (dead state published as live telemetry)** | `rust/jasper-host-clock/src/…:98,702,810,895-899,1806,1830-1831` — only `dll.reset()` is ever called (`:1176,1236,1440,1467,1692`); **`dll.update()` (`rust/jasper-clock/src/lib.rs:326`) has zero call sites**. Pinned as constants by `tests/test_fanin_host_clock_contract.py:55` (`'"dll":{"err_frames":0.00,"locked":false},'`) | The values are constants, so nothing *breaks* — but two fake fields sit in `/state` where an operator reads them as live diagnostics | The code comment at `:222-226` **already declares** the DLL is not ticked and is kept "for `dll_locked`/`dll_err_frames` diagnostics" — a rationale that contradicts itself, since an un-ticked DLL is not a diagnostic | delete the `dll` field, the two `/state` keys and the contract-test fragment | No — a deletion |
| 20 | p1-T14-2 | `staging.py` logs a metadata-write failure and still returns `status:"staged"` while the reader answers `not_staged` | Blocker | **CONFIRMED (Should-fix)** | `active_speaker/staging.py:1892-1903` — `atomic_write_json` failure is swallowed with a `logger.warning`, then `:1914 return payload` with `status == "staged"`; reader `:294-305` returns `status:"not_staged"` on `FileNotFoundError` | Needs ENOSPC/EROFS on the metadata write. **Fails closed** — downstream gates that require "staged" refuse, so the split-brain misleads the operator rather than loading an unvetted graph | none | on the exception, `payload["status"] = "unstaged"` (or re-raise) before `return` | Yes |
| 21a | p1-T06 | `output_hardware.py:660` `aplay -L` with no timeout on the DAC-vanished path | Blocker | **DOWNGRADE→Should-fix** | `jasper/output_hardware.py:660-666` `subprocess.run([aplay,"-L"], check=False, text=True, capture_output=True)` — **no `timeout=`**, and it is the only subprocess in the file | Reachable exactly as described: `main():1334-1336` calls it **only** when `probe_system_cards` returns no cards — the vanished-DAC case, and the shipped DAC is a USB dongle. **But** the sole caller is `python -m jasper.output_hardware` under `deploy/bin/jasper-audio-hardware-reconcile:393`, whose unit carries `TimeoutStartSec=50s` (`deploy/systemd/jasper-audio-hardware-reconcile.service:9`) — so the hang is bounded and surfaces as a failed unit | `TimeoutStartSec=50s` | `timeout=5` on the `subprocess.run` (+ catch `TimeoutExpired` → `return ""`) | No |
| 21b | p1-T06 | `audio_quality.env` has two writers | Blocker | **REFUTED → Nit** | `install.sh:1326-1330` writes it **only if absent** (a one-time default seed, guarded by `[[ ! -e ]]`); `jasper/audio_quality.py:175` is the sole runtime writer, and `_converter_env_text:114-118` **does** carry a `# Written by JTS /system audio quality control.` header | Seed-once + one runtime writer is the single-writer pattern, not a violation | the header names the writer, as AGENTS.md requires | none | No |
| 21c | p1-T06 | hardware classifier emits no `event=` at all | Blocker | **DOWNGRADE→Should-fix** | `jasper/output_hardware.py` — exactly **1** `log_event`/`event=` occurrence in the whole file, for the module that decides the box's output topology | Yes — this is the "easy to debug / observable" goal, not a failure | none | add `event=output_hardware.classified profile_id=… card_id=…` at the end of `main()` | No |
| 22a | p1-T03 | `openai_research.complete` poll loop has no attempt ceiling | Blocker | **REFUTED** | `research/providers/openai_research.py:96-100` — the `while` has no counter, but `:101-108` **explicitly handles** the scheduler's cancellation, spawns a best-effort server-side cancel (so billing stops) and re-raises; `reconnect_backoff_delay(attempt)` caps the poll rate. The ceiling is `scheduler.py:49 DEFAULT_MAX_RUNTIME_SEC = 300.0` applied at `:513` via `asyncio.wait_for` | Bounded at 300 s by design, at the right layer | the `wait_for` **is** the guard, and the cancel path is written to it | none — adding an inner cap would be a second, drift-prone bound (AGENTS.md: "do not defend hypotheticals") | No |
| 22b | p1-T03 | research + calibration_agent are two independent OpenAI clients | Blocker | **REFUTED** | `research/providers/openai_research.py:69-73` (`AsyncOpenAI`, background Responses API), `voice/openai_session.py:1475-1481` (`AsyncOpenAI`, realtime WebSocket), `cues/generator.py:332` (sync `OpenAI`, a cue-generation CLI). No `calibration_agent` client exists | Three **different** concerns with different APIs and lifecycles — not duplicate implementations of one concern | n/a | none | No |

---

## Summary of verdict changes

| verdict | count | items |
|---|---|---|
| **CONFIRMED** (at the source report's grade, Should-fix) | 9 | 2, 4, 6c, 8, 8c, 15, 16a, 17, 20 |
| **DOWNGRADE** (Blocker → Should-fix) | 11 | 1, 6a, 9, 10, 11, 14, 16b, 18b, 19, 21a, 21c |
| **DOWNGRADE** (Blocker → Nit) | 7 | 3, 6b, 7, 8b, 13, 18a, 18d |
| **REFUTED** | 5 | 5 (unreachable on shipped config), 18c (claim is factually wrong), 21b, 22a, 22b |

(33 rows because 6 of the 22 task items carried multiple sub-claims, graded separately.)
| **Status report, not a verdict** | 1 | 12 (ADR-0228 row 9, owner-owned) |
| **UPGRADE** | 0 | — |

**Nothing survives as a Blocker.** The closest three, in order:

1. **6c** — `travel_routes` bypasses the taint gate. The only item where a *declared security
   contract* is violated with no compensating control. Gate it on the owner having both packs on.
2. **8** — the polkit denial is real, per-unit, and the handler returns 200. It is the only
   confirmed **silent** partial failure in the set.
3. **10** — `SKIP_INSTALL=1` bypassing the identity guard collides with non-negotiable 4's
   wording, but the honest blast radius (a clobbered `~/jts` on the wrong Pi, zero runtime
   impact, `captures/`+`wake-events/` protected by the excludes) does not reach Blocker.

## NEW findings from the re-read (not in the source rows)

| sev | file:line | finding |
|---|---|---|
| **Should-fix** | `tests/` (absence) | **`redact_secrets` has zero tests repo-wide.** A secrets-adjacent module named by non-negotiable 3, with three consumers, has no pin at all — which is why item 9's regex defect survived. One parametrized pin is the fix. |
| **Should-fix** | `accessories/bridge.py:728-742` + `:701` | Beyond item 16a: the reap loop only runs after `await events.get()`, and `_maybe_start` fires only on udev `add`. A **still-plugged** accessory whose reader task died is never reaped and never restarted, while `/state` reports the bridge healthy. Strictly worse than "unsupervised". |
| **Should-fix** | `control/handlers/system.py:497-517` | The restart/reboot/poweroff path uses `Popen` for **every** action with no returncode check, so *any* systemctl failure (not just the usbsink-volume one) returns HTTP 200. Item 8 is one instance of a general pattern. |
| **Nit** | `active_speaker/commissioning_apply.py:889` | The `KNOWN DEFECT (issue #2202)` comment is **accurate and load-bearing** — but AGENTS.md forbids issue-number-only prose in code without a `why`-pointer to a decision record. It should cite `ADR-0228 row 9`, which is where the owner ruling actually lives. |
| **Nit** | `camilla_config_contract.py:687-690` | On a **duplicated** `volume_limit` key the parser `result.pop("volume_limit", None)`s it so "safety callers fail closed". Correct, but the fail-closed contract is asserted only in a comment — no caller is pinned to actually refuse on the missing key. |

## Coverage

**Opened and re-read:** `jasper/voice/output_gate.py`, `jasper/voice_daemon.py` (measurement-pause,
acquire, begin-turn, manual-start, wake-late-cancel paths), `jasper/voice/input_policy.py`,
`jasper/voice/_supervisor.py`, `jasper/wake_corpus/recording_backend.py`, `jasper/wake_events.py`,
`jasper/tools/{__init__,packs,travel_routes,home_assistant}.py`, `jasper/camilla.py`,
`jasper/camilla_config_contract.py`, `jasper/sound/camilla_yaml.py`,
`jasper/active_speaker/camilla_yaml.py`, `jasper/web/sound_setup.py`,
`jasper/active_speaker/web_commissioning.py`, `jasper/active_speaker/{staging,commissioning_apply}.py`,
`jasper/control/{handlers/system.py,handlers/voice.py,server.py,restart_broker.py}`,
`jasper/local_sources/registry.py`, `deploy/polkit/49-jasper-control.rules`,
`deploy/systemd/jasper-control.service`, `deploy/jasper-web{,-streambox}.service`,
`deploy/bin/jasper-render-asound-conf`, `deploy/lib/jasper-asound-render.sh`, `deploy/install.sh`
(asound block), `jasper/audio_quality.py`, `jasper/secret_redaction.py`,
`jasper/cli/doctor/{_shared,voice}.py`, `scripts/deploy-to-pi.sh`,
`jasper/active_speaker/crossover_v2/{__init__,contracts}.py`, `jasper/active_speaker/branch_chain.py`,
`jasper/correction/runtime_integrity.py`, `jasper/route_latency/status_socket.py`,
`jasper/audio_measurement/calibration.py`, `jasper/accessories/{bridge,supervisor,registry}.py`,
`jasper/identity.py`, `jasper/output_hardware.py`, `jasper/research/providers/openai_research.py`,
`jasper/research/scheduler.py`, `rust/jasper-outputd/src/main.rs`,
`rust/jasper-fanin/src/{main.rs,tts.rs,config.rs}`, `rust/jasper-host-clock/src/*`,
`rust/jasper-clock/src/lib.rs`, `tests/test_systemd_hardening.py`, `tests/test_voice_output_gate.py`,
`tests/test_fanin_host_clock_contract.py`, `docs/adr/0228-…`.

**Executed:** `redact_secrets` reproduction; `local_source_audio_refresh_units()` vs
`restart_broker` allowlists set-difference; `python3 -X importtime` on
`jasper.active_speaker.crossover_v2` and `jasper.identity`; `gh`/MCP read of PR #4120.

**Skipped and why:** no hardware — every reachability call is static plus the unit files and
polkit rule, so the polkit denial and the `aplay -L` hang are argued from the rule text and
`TimeoutStartSec`, not observed. Item 12 was scoped to status reporting per the task. `p1-T25`,
`p1-T27`, `p1-T22`, `p1-T17`, `p1-T15`, `p1-T10`, `p1-T07`, `p1-T04` were out of scope (no
Blocker rows assigned to this pass).
