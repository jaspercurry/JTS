# P3 — adversarial verification of the Phase 2 scenario/lens BLOCKERS

Method: every verdict below rests on a re-read of the cited `file:line` at HEAD `2d571e6b8`, plus
three executed experiments (stub-PATH deploy under a pty; the pure peering state machine driven
directly; the volume-curve predicates evaluated numerically). Scripts in `scratchpad/P3-seams/`.
HEAD advanced `2d571e6b8` → `a9d75c025` mid-session; that commit is docs-only (the review reports),
so every citation below was re-spot-checked and holds at `a9d75c025`. Repo left untouched.
Reachability is judged on the owner's shipped config: full-profile Pi 5 + XVF3800 + Apple dongle;
streambox Zero 2 W; multi-speaker household with peering optional.

Headline: **9 CONFIRMED (2 upgraded), 4 DOWNGRADED, 1 partly refuted.** The two findings that
most deserve the owner's attention are **N** (a proven, self-sustaining 1 Hz write storm that
leaves the speaker silent at 1 % volume) and **F/NEW-1** (a proven state-machine gap that makes
the *better-positioned* speaker lose arbitration whenever the peer's multicast lands first).
The item graded Blocker on the non-negotiable-1 clamp (**D**) is a door with **no key**.

---

## Verdicts

| id | claim (as filed) | source | verdict | what I re-read / ran | reachability on shipped config | existing pin/guard? | cleanest fix (≤1 line) + test? |
|---|---|---|---|---|---|---|---|
| **A** | default interactive-sudo fallback silently disables identity + direction guards, OOM scan, manifest verification, health — no flag, no opt-in | p2-S4 #2 | **CONFIRMED** | `deploy-to-pi.sh:167-199` (fallback), `:238` (direction skip), `:625` (identity skip), `:763` (OOM skip), `:985` (verify+health skip), `:682-687` (rsync runs regardless). **Ran** the stub experiment (`P3-seams/{bin,repo,drive_sm.py}`): non-tty → exit 1, **0 rsync calls**; under `script -qec` pty → prints `speaker identity: skipped` + `deploy direction: skipped`, then **1 rsync `--delete`** and `install.sh` onto a peer_id that does **not** match `.env.local` and a commit that is a **downgrade** vs the installed manifest — no `JASPER_DEPLOY_ALLOW_DOWNGRADE`, no `JTS_ACCEPT_NEW_IDENTITY` | **Yes.** Fires exactly when passwordless sudo is absent + a human is at a tty. BRINGUP.md:250-256 names the lab-Pi case as a real recurring one. Owner's main Pi has Phase 2.5 done; a **second/lab speaker** is the live path | No pin on the bypass branch. `test_lib_deploy_direction.py:450,531` and `test_deploy_oom_collateral.py:165` all hard-set `SUDO_INTERACTIVE=0`; `test_deploy_wiring_guards.py:513-520` pins the *skip* as intended (and does it by regexing source text, itself an AGENTS.md violation) | Capture the manifest + peer_id over a second **non-tty** `ssh -o BatchMode=yes` channel (only `sudo` needs the pty) so the guards survive the fallback. **Behavior test: yes** — one pin that a downgrade aborts under `SUDO_INTERACTIVE=1` |
| **B** | deploy-health red exits 0; no path makes a red box fail the deploy | p2-S4 #3 | **DOWNGRADE → Should-fix** | `install.sh:2158-2201` — `run_doctor_summary` ends both arms on `echo`/`return 0`; `:2333`/`:2288` put `write_build_manifest` (status=ok) *before* it; `deploy-to-pi.sh:539-575` — `surface_system_health`'s own header says **"ADVISORY — does not gate the deploy"** and the remote command ends `\|\| true`; `:1009` `==> Done.`. **Answer: no path.** The only non-zero tails are `verify_manifest_advanced` (`:522`) and `OOM_PRODUCTION_HIT` (`:997`) — neither is health | n/a (design) | **AGENTS.md:131-132 does not promise a health gate** — it tells the operator to run `jasper-doctor` on the Pi as a *separate* verify step. `surface_system_health:540-547` states the reason for non-gating (a mic-less daemon reads ✗ until Workstream C reclassifies) and names its owner. Adding a gate that fires on a known false-positive class is the nanny AGENTS.md forbids | Not a gate. Emit one machine-readable line (`event=deploy.health status=red failures=N`) beside the banner so an unattended agent can see it. **Behavior test: no** |
| **C** | `install.sh:2167` guards `:2173`, so the stdlib "venv is broken" probe is unreachable when the venv is broken | p2-S4 #4 | **DOWNGRADE → Nit (rationale is the bug, not the code)** | `install.sh:2167` `if [[ ! -x /opt/jasper/.venv/bin/jasper-doctor ]]; then return 0; fi` guards the whole function incl. the `build_swap_required → jasper-deploy-health` arm at `:2172-2183`. **But** `deploy/bin/jasper-deploy-health:7-16` says its rationale is **1 GB RAM**, not a broken venv — the code gates on RAM correctly. The "venv broken" framing comes only from `docs/DEEP-AUDIT-2026-08-25.md:149`, a prior audit report that the code contradicts. Also `deploy-to-pi.sh:558-570` runs `jasper-deploy-health` on low-mem hosts with **no `-x jasper-doctor` guard**, so the wrapper covers the case anyway | Low: reaching `run_doctor_summary` with the console script absent needs an install that succeeded (`set -e`) yet left no entry point | none | Delete the stale sentence in `docs/DEEP-AUDIT-2026-08-25.md:149` (the doc is wrong, the code is right). **Behavior test: no** |
| **D** | nothing reads the RUNNING graph's `volume_limit`; doctor reads the persisted file; `/sound/live-draft` writes through `set_active_config_raw`, which leaves `config_file_path` unchanged | p2-S2 Blocker (+p1-T05 #1) | **DOWNGRADE → Should-fix. Door-without-detector, but the door has NO KEY.** | Detector gap **confirmed**: `cli/doctor/audio.py:1065-1112` reads `evidence.camilla_config_path()` (the statefile path); `camilla.py:891-922` `set_active_config_raw` uploads without validating and `:939-941` documents that it deliberately leaves `config_file_path` alone; no `volume_limit` string exists in `graph_evidence.py`, `graph_safety.py` or `sound/live_edit.py`. **Constructibility refuted**: `camilla_config_contract.py:438-458 ensure_volume_limit_db` raises above 0 dB and has exactly two callers — `sound/camilla_yaml.py:456` (unconditional, on every `emit_sound_config`) and `active_speaker/camilla_yaml.py:414-419` (`_assert_volume_limit`, at `:2110,:2280,:2682,:3303,:3591,:3816`). Grepped every `emit_sound_config(` call site: `sound/graph_carrier.py:258,343` and `multiroom/leader_config.py:288` pass **no** `volume_limit_db`; only `active_speaker/camilla_yaml.py:3990` forwards one, already asserted. `deploy/camilladsp/outputd-cutover.yml:18` = `0.0`. **No shipped surface — `/sound/` live-draft, `/eq/`, correction, crossover, `jasper-sound` — accepts `volume_limit` as input** | Not reachable today. Becomes reachable the moment any future emitter bypasses the two funnels | `path_safety.py:473,554,676,685,710` gate staged/restore selection on `volume_limit_ok`; `runtime_contract.py:1172,1310,3694` carry it in `details` but it **never gates `allowed`**. All of these classify config *text*, and only `multiroom/follower_config.py:530` + `bass_extension/__init__.py:143` feed them `get_active_config_raw()` — so the running graph *is* classified on the bass-extension/bond paths, just never for `volume_limit` | Have the doctor check compare against `get_active_config_raw()` when it is readable, falling back to the file. **Behavior test: yes** — non-negotiable-1 tier warrants the pin even though the door has no key (ADR-0101) |
| **E** | fanin `max_peak_dbfs` has no upper bound; `volume_limit` caps the fader not the signal; the passive graph has no Limiter | p2-S2 re-grade of p1-T19-2 #8 | **CONFIRMED mechanically, DOWNGRADE → Nit. It is a knob nobody turns.** | `rust/jasper-fanin/src/config.rs:1131-1134` reads `JASPER_OUTPUTD_ASSISTANT_MAX_PEAK_DBFS` via `env_f32` with **no clamp**; `rust/jasper-tts-protocol/src/loudness.rs:446-448` `peak_cap_gain = max_peak_dbfs - source_peak_dbfs`, then `:622-626 sanitize_tts_gain_db` enforces **only a floor** (`MIN_TTS_GAIN_DB = -60`, `:21`); `jasper/sound/camilla_yaml.py` emits **0** `Limiter` blocks (vs 3 in `active_speaker/camilla_yaml.py`). **Writers of that env var:** grep of the whole tree → only `.env.example:140` and `docs/audio-paths.md:447`, both `-3.0`. `python-runtime.sh:357-364` seeds `/etc/jasper/jasper.env` from `.env.example`, so the shipped value is safe. `jasper-fanin.service:140` pulls it in. The design is otherwise sound: with `max_peak_dbfs ≤ 0` the cap bounds the *output peak*, not the gain, and provider-supplied `source_peak_dbfs` is range-checked (`lib.rs:319-321`) | Only via a hand-edit of `/etc/jasper/jasper.env` to a **positive** value. Not reachable from any UI, wizard, or CLI | none | `.min(0.0)` on the `env_f32(...)` result at `config.rs:1131-1134`. **Behavior test: no** (a Rust unit assert is enough) |
| **F** | peering fail-open: 3 LocalWake/PeerClaim combos return no action so the ARBITRATE future never resolves; voice's 0.5 s client timeout beats the daemon's 0.65 s fail-open; rank.py effectively dead | p2-S5 #1/#2 | **CONFIRMED (state-machine half **UPGRADED**, timeout half **DOWNGRADED**)** | **Ran** `P3-seams/drive_sm.py` against the pure machine. `state.py:284-313` — SUPPRESSED + weak LocalWake → `[]`; `:314-320` CANDIDATE/WINNER/ACTIVE + LocalWake → `[]`. `daemon.py:363-391`: the future resolves **only** from `StartSession`/`StandDown` (`:461-464`), so `[]` = hang → `voice_daemon.py:3944-3945` default `timeout=0.5` (the ARBITRATE call at `:4011` passes none) fires → `:4012-4013` returns **WIN**. So *SUPPRESSED + weak wake inverts its own intent from LOSE to WIN* — the suppressed speaker answers anyway. **Timeout half downgraded:** `config.py:70` `MAX_ARB_WINDOW_MS=500` → `daemon.py:96` fail-open at **0.65 s**, which the 0.5 s client can never observe — so `ARBITRATE_RPC_TIMEOUT_SEC` and its careful 0.15 s margin comment (`:83-95`) are **unreachable on the wire**; both sides fail open to WIN, so it is redundant machinery, not a hazard. At the **default** `DEFAULT_ARB_WINDOW_MS=150` (`config.py:60`) the client's 0.5 s is comfortably above the real decision; the race needs a hand-tuned `JASPER_PEER_ARB_WINDOW_MS > ~350` | **Gated on peering being ON.** `config.py:298` defaults `JASPER_PEERING=off`; `server.py:2268-2270` makes the daemon a no-op when off. Owner's household: reachable only once the `/rooms/` Speakers page turns it on | `tests/test_peering_state.py:304-309` docstring **already names the multi-waker race as a known, deliberately-unasserted enforcement gap**. So: acknowledged, not pinned | Make `_on_local_wake` join an in-flight CANDIDATE instead of returning `[]` (see NEW-1), and drop `ARBITRATE_RPC_TIMEOUT_SEC` down under the client's 0.5 s (or raise the client's). **Behavior test: yes** |
| **G** | wake-leg tasks are bare `create_task`; a raise kills a leg silently while `/state.voice.wake_legs` keeps listing it | p2-S5 #4 | **CONFIRMED, UPGRADED** | `voice_daemon.py:2373-2386` — `asyncio.create_task(...)` ×2 loops, **no `add_done_callback`, no `gather`, no supervisor**. The only later touch is `:2500-2505`, which cancels and then `await`s each task inside `except (asyncio.CancelledError, Exception): pass` — so the stored exception is **retrieved and discarded**, and because `leg_tasks` holds a reference throughout, asyncio's "Task exception was never retrieved" warning never fires either. **The traceback reaches the journal nowhere, ever.** And `:4788` publishes `"wake_legs": list(self._legs)` — the *configured* dict — under a comment at `:4783` that calls it **"Actually-armed wake legs (runtime truth…)"**, which is false the moment a leg task dies | **Yes**, on the owner's full profile: `_legs` is non-trivial exactly on an XVF3800 box (on/off/dtln/raw0/…), so a dead leg is a silently halved wake surface | none | `t.add_done_callback(_log_leg_death)` at `:2379`, and publish the live task set at `:4788`. **Behavior test: yes** |
| **H** | nothing reports wake recency or rate; a mic streaming silence looks healthy in /state and doctor | p2-S5 | **CONFIRMED (with one honest carve-out)** | `voice/input_presence.py:26` states it itself: **"This is a start gate, not a runtime guarantee"**; it only answers "was there a mic at boot". `mic_presence.py:221` / `state_aggregate.py:1264-1303` publish that same static record. `cli/doctor/wake.py` has exactly two checks (`:42` models, `:277` legs configured) — neither is temporal. `memory.py:605-638 check_wake_events_storage` is a **disk-space** check. **Carve-out (partial refutation):** frames-stopping *is* caught — `voice_daemon.py:2436-2437` bumps `watchdog.Heartbeat` per frame, 5 s stale threshold (`watchdog.py:61`) → systemd restart. **Silence is not**: all mic-level telemetry is wake-triggered — `_ring_noise_floor_dbfs` is documented "computed once at fire time (never per frame)" (`:685`) and the `mic_rms_dbfs_*` columns (`:790-810,3526,3564`) are written per wake event. **No detector exists for "frames arrive but are all zeros"** | **Yes** — the classic XVF3800 failure (chip alive, mixer ch2-5 muted) presents exactly as frames-of-silence. `install.sh:2163-2166` names that failure by name as the thing doctor exists to catch | none | Publish `voice.last_wake_at` + a rolling idle-frame RMS into `/state`, and one doctor check that fails when RMS has been at the floor for > N hours with the mic unmuted. **Behavior test: yes** |
| **I** | `StartLimitBurst=4 × RestartSec=2` → 8 s to `StartLimitAction=reboot`; no config-error park; a held port 8780 reboots the box; an unplugged mic reboots the box | p2-L2 #1 | **SPLIT: "held port" CONFIRMED → Should-fix; "unplugged mic" REFUTED** | Units read: `jasper-control.service:18-20,56-58` and `jasper-aec-bridge.service:53-55,97-103` — both `Burst=4 / Interval=300 / RestartSec=2 / Action=reboot`, **neither has `SuccessExitStatus`** (contrast `jasper-voice.service:219` `SuccessExitStatus=66 78`, which is the park idiom this repo already owns). **Held port CONFIRMED:** `control/server.py:2178` `main()` → `:2241` `build_server(...)` → `ControlHTTPServer((host, port), …)` (`:2131-2142`) binds inline with **no try/except anywhere in `main`** → `EADDRINUSE` escapes → exit 1 → reboot ladder. **Mic REFUTED:** `jasper-aec-bridge.service:47` `ConditionPathExists=/run/jasper-aec-reconcile/aec-bridge-ready`; a failed Condition is not a failed start, so it does not consume start-limit budget, and `deploy/bin/jasper-aec-reconcile:197` `rm -f` clears the marker on mic removal — p1-T07's reconciler **wins**, with a narrow race (the bridge must fail 4× inside the reconciler's udev latency). The five permanent faults at `cli/aec_bridge.py:1077,1106,1150,1170,1176` are real but all sit **behind** that Condition | Held port: reachable (a stray manual `jasper-control`, or a deploy race where the old process has not released 8780) | **`deploy/bin/jasper-bootloop-guard`** — 3 boots inside 3600 s (`:70`) writes runtime drop-ins with `StartLimitAction=none`. So the blast radius is bounded to **~3 reboots**, then the box stays up and reachable. This is why the item is Should-fix, not Blocker | Wrap `build_server(...)` at `server.py:2241` in `try/except OSError` → log + `return 78`, and add `SuccessExitStatus=78` to the unit. **Behavior test: yes** |
| **J** | `outputd.env` two-writer with one-sided lock: bash reconciler cp→mutate→validate→mv (`:600→:846`) unlocked, vs `coupling_reconcile.py` under `/run/jasper-fanin-coupling.lock` | p2-L2 #2 | **CONFIRMED as LOST UPDATE, not torn write. DOWNGRADE → Should-fix** | `deploy/bin/jasper-audio-hardware-reconcile:600-613` (`stage_outputd_env`: cp → stage) and `:838-849` (chown/chmod → **`mv`**) — grep for `flock` in that file: **zero hits**. `coupling_reconcile.py:1739-1741 _write_env_text` → `atomic_write_text`. **Both ends rename atomically, so a torn read is impossible.** The hazard is read-modify-write interleaving: `coupling_reconcile.py:889-919` reads the snapshot then writes it back, and the bash script's own read→`mv` window spans a cp, a mutate and a **Python validator** (hundreds of ms). **The documented spine is safe** — `:852-882` calls `do_converge_content_format` (which starts the bash unit) *after* the Python write, so the ordered path never interleaves. The exposure is the **unordered** path: a udev/boot/install-triggered run of the bash unit landing inside the Python window | Narrow but real on the owner's box: the **Apple dongle** hotplug is a udev trigger, and re-plugging it during a `/sources/` toggle is the interleave | The file's own comment at `coupling_reconcile.py:899-903` already concedes **"its two halves have two writers"** and logs the heal — which contradicts its module docstring at `:18` ("`_outputd_actions` is the single writer") and AGENTS.md's single-writer rule. **The p2-S2 layering note narrows the count from 3 to 2, not to 1:** `jasper-outputd.service:97` reads `grouping-outputd.env` as a *separate* `EnvironmentFile=`, so multiroom is not a third writer of *this file* — the bash/Python pair remains | Put both writers behind one **new** `flock /run/jasper-outputd-env.lock` (not the coupling entry lock — the bash unit is started from inside that one). **Behavior test: yes** |
| **K** | `systemctl reload avahi-daemon` granted to neither polkit rule, rc dropped; and POST `/system/audio-quality` writes `/var/lib/jasper-asound` from a `ProtectSystem=strict` unit whose `ReadWritePaths` omit it | p2-S1 new | **BOTH CONFIRMED; deletion of the reload IS safe** | **Avahi:** `peering/avahi.py:126-138` `subprocess.run(["systemctl","reload","avahi-daemon"], check=False)` with rc discarded; `jasper/avahi_service.py:114-134` is the shared owner for the rename path (`web/speaker_setup.py:298-305` → `control_advert.py:146-189`). `deploy/polkit/49-jasper-control.rules:80-100` scopes `manage-units` to `MANAGED_UNITS ∪ START_ONLY_UNITS`; grep for `avahi` across `deploy/polkit/*.rules` → **0 hits** → `NOT_HANDLED` → denied → silent no-op. **Safe to delete:** both docstrings (`avahi.py:127-128`, `control_advert.py:43-49`) state avahi inotify-watches the directory, and the call is described as best-effort belt-and-braces. **Audio-quality:** `deploy/bin/jasper-render-asound-conf:15` `OUTPUT=/var/lib/jasper-asound/asound.conf`; `jasper-control.service:115,139,160` = `User=jasper-control`, `ProtectSystem=strict`, `ReadWritePaths=/var/lib/jasper /var/lib/jasper-intsecrets /etc/avahi/services` — **`/var/lib/jasper-asound` is absent** and grep confirms no drop-in grants it → the `> "$tmp"` at `:56` fails EROFS | Avahi: every peering enable and every `/speaker` rename. Audio-quality: **every** POST — the feature cannot succeed on the shipped unit | The audio-quality failure is at least **honest**: `control/handlers/system.py:346-352` catches `SubprocessError` and returns **502**, so it is a broken feature, not a lie. **One p2 sub-claim refuted:** the `_asound_path` default mismatch (`audio_quality.py:52` `/etc/asound.conf` vs the script's `/var/lib/jasper-asound/…`) is **not** a bug — `install.sh:1341` symlinks the former to the latter | Two independent one-liners: delete `_reload_avahi()`'s body (or the call at `avahi.py:123`); add `/var/lib/jasper-asound` to `jasper-control.service:160`. **Behavior test: no** for the deletion; **yes** for the ReadWritePaths (a unit-vs-writer path pin) |
| **L** | six hand-rolled `STATUS\n` readers vs `route_latency.status_socket` (three dropped both deadline and cap); `renderer.py:112` mux connect with no timeout; `usage.db` full `strftime()` scan on every wake | p2-L2 | **CONFIRMED — count is exactly right; `renderer.py` and `usage.db` are the sharp ends** | Enumerated all 12 `STATUS\n` sites. **Three sync readers use a per-operation `settimeout` (not a total deadline) and accumulate `chunks` with no byte cap:** `audio_validation.py:505-517`, `control/airplay_health.py:1690-1700`, `correction/runtime_integrity.py:101-112`. Each is an unbounded-memory primitive on a 1 GB Pi against a dribbling peer. p2's "three" is **exactly right** — my first pass counted six because the asyncio sites' implicit limits are invisible to grep. **Partly refuted:** `control/grouping_supervisor.py:445-459` is clean (`wait_for` on connect + shared `read_status_body`), and `mux.py` / `uds.py` ride `readline()`'s 64 KiB limit. **`renderer.py:112` CONFIRMED and the worst of the three**: `asyncio.open_unix_connection(MUX_CONTROL_SOCKET_PATH)` with **no** timeout wrapper — the `asyncio.TimeoutError` in the `except` tuple at `:115-116` is **dead**, nothing there can raise it — and the read *is* bounded (`:130` `asyncio.timeout(1.0)`), so the hole is connect-only. It is reached **every tick** by `VolumeObserver._run` → `_tick` → `VolumeCoordinator._active_source`, so a wedged jasper-mux hangs the volume observer indefinitely. **`usage.db` CONFIRMED on the hot path**: `voice_daemon.py:3512 self._spend_cap.allowed()` runs on **every wake fire** → `usage.py:1106 _padded_spend` → `:823-833 spend_last_24h_usd` → `WHERE strftime('%s', started_at) >= ?` — a function on the column, so unindexable, and `usage.py` contains **zero** `CREATE INDEX` and **zero** retention/DELETE/VACUUM | All three reachable on the owner's full profile. The spend scan is **on by default**: `.env.example:203` seeds `JASPER_DAILY_SPEND_CAP_USD=1.00`, so `SpendCap.disabled` is False and the scan runs on every wake, over a table that grows forever | **Two** shared helpers already exist for one concern — `route_latency/status_socket.py:67` (sync, total deadline + cap) and `control/uds.py:181 read_status_body` (async). AGENTS.md's duplication rule ("never add a third") is already at two | `renderer.py:112`: wrap the connect in `asyncio.wait_for(..., 1.0)`. `usage.py:414`: store an integer epoch column + `CREATE INDEX`. Route the three sync readers through `read_status_socket`. **Behavior test: yes** for the renderer connect timeout |
| **M** | `initialize()` / `maybe_reconcile_camilla()` / `apply_active_source_transition()` have their only production caller inside jasper-voice, which streambox disables → no boot volume restore or drift repair on streambox | p2-S6 | **CONFIRMED, with one caller carve-out** | `systemd-units.sh:1121-1127` disables `jasper-voice.service` on streambox. Callers grepped across all of `jasper/` minus tests: `initialize()` → **only** `voice/daemon_main.py:834`; `apply_active_source_transition()` → **only** `volume_observers.py:159`; and `VolumeObserver` is constructed **only** at `voice/daemon_main.py:855`. So the boot restore (`volume_coordinator.py:437-457`, incl. the idle-reset and the safety regression) and the 1 Hz drift repair are both **dead on streambox**. **Carve-out:** `maybe_reconcile_camilla()` has a second caller at `web/sound_setup.py:1234`, inside jasper-web, and `systemd-units.sh:1322` confirms `/sound/` **is** live on streambox — so drift *can* be repaired there, but only when a human saves EQ. That is not a boot restore and not drift repair | **Yes** — the owner runs a streambox Zero 2 W. It renders AirPlay/Spotify to a DAC and has a household volume; without `initialize()` its CamillaDSP fader boots to whatever the config file says, not the persisted level | none | Start a `VolumeObserver` (or at minimum call `initialize()`) from jasper-control when the profile is streambox. **Behavior test: yes** |
| **N** | 1 % volume asserts `main_mute` via `_set_camilla` (`:2602`) vs `_write_camilla_db_with_mute` (`:2077`) because `percent_to_db(1)==percent_to_db(0)`, and the reconciler then re-writes every tick | p2-S6 | **CONFIRMED and UPGRADED to a proven self-sustaining loop — but the p2 row names the wrong function as the muter** | **Ran** the predicates: `percent_to_db(0) == percent_to_db(1) == -50.0`; `MUTE_DB_EPSILON = 1e-06`. `volume_coordinator.py:1993-1994 _main_mute_for_level(1)` = `1 <= 0` = **False**; `:1996-1998 _main_mute_for_db(-50.0)` = `-50 <= -50+1e-6` = **True**. So the **dB path mutes at 1 %, the level path does not** — the inverse of the p2 attribution: `_set_camilla` (`:2600-2601`) uses the *level* predicate and is correct; `_write_camilla_db_with_mute` (`:2066-2079`) uses the *dB* predicate and is the muter. **The loop, traced through `maybe_reconcile_camilla` (`:1757-1897`):** at `listening_level == 1`, `expected_mute = False` (`:1811`, level predicate) but the reconciler's own write at `:1893` goes through `_write_camilla_db_with_mute(expected_db=-50.0)`, which asserts `main_mute = True`. Next tick `mute_drift` (`:1822-1825`) is True → writes again → asserts mute again → **forever, at 1 Hz, logging `event=volume.reconciled` every tick** (`:1875`) into persistent journald. Two user-visible consequences: the speaker is **silent at 1 %** — the level `volume_curve.py:102-104` explicitly documents as "the audible calibration floor" — and the box burns a Camilla round-trip + a journal line every second | **Yes**, on any full-profile box where the household lands on 1 % (a `adjust_listening_level(-1)` from 2 %, or a remote twist to the bottom). Not reachable on streambox — because of **M** the reconciler never runs there | none | Give `_write_camilla_db_with_mute` an explicit `mute` parameter and pass `expected_mute` from the reconciler at `:1893`, so the **level-domain** intent decides `main_mute` instead of the lossy dB round-trip. **Behavior test: yes** — pin that `listening_level == 1` converges in one write and leaves `mute_drift` False |

---

## New blockers / findings this pass turned up

| # | sev | file:line | what | evidence |
|---|---|---|---|---|
| **NEW-1** | **Blocker** | `jasper/peering/state.py:314-320` | **The local wake report is silently dropped whenever the peer's WAKE multicast arrives first — so `rank.py` never ranks it and the nearer speaker unconditionally loses.** This is the sharp form of the "rank.py is dead" claim, and it is worse than filed. | Driven directly (`P3-seams/drive_sm.py`, CASE 4): `PeerWake(epoch=E9, report=peer{score 0.30, snr 3 dB, rms −45})` at t=0 → `_on_peer_wake`'s IDLE branch (`:326-336`) adopts the foreign epoch and enters CANDIDATE, with the docstring at `:315-316` promising *"so our local detector can join when it fires."* Then `LocalWake(score 0.99, snr 30 dB, rms −12)` at t=0.02 → state is CANDIDATE → `_on_local_wake` falls to `return []` (`:320`). Printed: `local report joined? **False**`. At `TimerFired(TIMER_ARB_WINDOW)` the machine emits **`StandDown`** → LOSE. The two comments in the same file contradict each other, and on a LAN the multicast (~1 ms) beats detection jitter ("30-150 ms on Pi 5", `config.py:55-56`) essentially always — so the speaker that *heard the user better* is the one that stands down. Fix: let a CANDIDATE with no local report of its own record that report and `BroadcastWake` instead of returning `[]`. |
| **NEW-2** | **Should-fix** | `jasper/sound/profile.py:44-46` vs `jasper/camilla_stereo_prefix.py:196-198` | A **safety comment that is false**. `profile.py` says of the ±12 dB EQ bands: *"the headroom preamp auto-attenuates, so boosts stay clip-safe."* `camilla_stereo_prefix.py` says the opposite three lines into the same pipeline: *"Preference boosts apply at unity: a +N dB band raises only that band."* | The `room_headroom` preamp (`camilla_stereo_prefix.py:180-186`) compensates **only** `room_peqs`; `sound_preamp` (`:215-217`) carries only the caller's `output_trim_db`, itself clamped attenuating (`:213 max(0.0, …)` emitted as `-trim_db`). User EQ boost — up to 12 dB × 8 bands, clamped at `profile.py:213,328` — reaches the DAC uncompensated. Real ceiling is digital full scale (so this is clipping, not an SPL breach — non-negotiable 1 holds), but AGENTS.md is explicit that an unverifiable comment misleads agents more than a missing one. Fix: delete the clause in `profile.py:45`. |
| **NEW-3** | **Should-fix** | `jasper/voice_daemon.py:4783-4788` | `/state.voice.wake_legs` is labelled **"Actually-armed wake legs (runtime truth…)"** but publishes `list(self._legs)` — the configured dict, which never changes after construction. | Compounds **G**: the one observability field an operator would consult to notice a dead leg is the field that cannot notice one. Fix: publish the set of leg tasks that are still `not done()`. |
| **NEW-4** | **Nit** | `jasper/route_latency/status_socket.py:67` + `jasper/control/uds.py:181` | **Two** shared `STATUS\n` helpers for one concern (sync and async), plus six hand-rolls. AGENTS.md: "Two implementations of one concern in reach: converge them or open an issue — never add a third." | Context for **L**: the fix is not "write a third helper", it is to route the three uncapped sync readers into `read_status_socket` and open an issue on the sync/async pair. |
| **NEW-5** | **Nit** | `BRINGUP.md:202-206` | The doc says the deploy scripts *"preflight `sudo -n true` … and **refuse to proceed without it**"*, which is only true off a tty. Its own options table two paragraphs down concedes the interactive path exists — and **neither** place mentions that the interactive path ships without identity, downgrade, OOM, manifest or health verification. | Context for **A**: the operator choosing "keep a sudo password" (`BRINGUP.md:224`) is not told what they are giving up. Fix: one sentence in that table cell. |

---

## What only hardware/runtime can prove

- **J**: whether the udev-triggered bash reconciler ever actually lands inside `coupling_reconcile.py`'s read→write window. The window is a few ms; I proved the *shape*, not a real collision.
- **I**: whether the reconciler's `rm -f` of `aec-bridge-ready` reliably beats 4 bridge restarts (8 s). Reasoned, not measured.
- **N**: I proved the predicate divergence and traced the latch through the reconciler's two gate blocks by reading. The 1 Hz storm itself wants one `journalctl -u jasper-control | grep volume.reconciled` at 1 % on a real box.
- **H**: whether an XVF3800 with ch2-5 muted really presents as frames-of-silence rather than frames-stopping (which the watchdog *does* catch).

## Coverage

Opened and read: `scripts/deploy-to-pi.sh` (1-1010), `scripts/_lib.sh` (27-130), `deploy/install.sh` (1276-1342, 2140-2345), `deploy/lib/install/{python-runtime,systemd-units}.sh` (cited ranges), `deploy/bin/{jasper-deploy-health,jasper-audio-hardware-reconcile,jasper-render-asound-conf,jasper-aec-reconcile,jasper-bootloop-guard}`, `deploy/systemd/{jasper-control,jasper-aec-bridge,jasper-voice,jasper-outputd,jasper-fanin}.service`, `deploy/polkit/49-jasper-control.rules`, `deploy/camilladsp/outputd-cutover.yml`, `BRINGUP.md` (196-260), `AGENTS.md`, `docs/DEEP-AUDIT-2026-08-25.md:143-155`; `jasper/{camilla,camilla_config_contract,camilla_stereo_prefix,volume_coordinator,volume_curve,voice_daemon,renderer,mux,usage,audio_quality,control_advert,avahi_service,mic_presence,watchdog}.py`, `jasper/sound/{camilla_yaml,graph_carrier,live_edit,profile}.py`, `jasper/active_speaker/{graph_evidence,environment,path_safety,runtime_contract,camilla_yaml}.py`, `jasper/peering/{state,daemon,config,avahi}.py`, `jasper/control/{server,state_aggregate,grouping_supervisor,airplay_health,uds}.py` + `handlers/system.py`, `jasper/cli/{aec_bridge}.py` + `cli/doctor/{audio,wake,memory}.py`, `jasper/fanin/coupling_reconcile.py`, `jasper/multiroom/follower_config.py`, `jasper/voice/input_presence.py`, `jasper/route_latency/status_socket.py`, `jasper/correction/runtime_integrity.py`, `jasper/audio_validation.py`; `rust/jasper-fanin/src/config.rs`, `rust/jasper-tts-protocol/src/{loudness,lib}.rs`; `tests/{test_peering_state,test_deploy_wiring_guards}.py`.

Executed: the stub-PATH deploy experiment under a pty and non-tty (`P3-seams/{bin/ssh,bin/rsync,repo/}`); the pure peering state machine, 4 scenarios (`P3-seams/drive_sm.py`); the volume-curve predicate evaluation.

Not re-verified (accepted from p2 as filed, outside this brief): p2-S4 #1 (`SKIP_INSTALL`), #5-#9; p2-L2 items beyond #1/#2 and the socket/usage set; every p2 Should-fix and Nit.
