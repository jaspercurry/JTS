# W3.5 — Jargon and copy sweep (Codex prompt)

Read [`README.md`](README.md) (the charter) first; it is binding. Then
read this file completely before writing code. Prereq: W3.3 merged
(this workstream edits the same crossover-envelope copy W3.3
restructured — land after it, not concurrently, to avoid a rebase
fight over the same lines).

## Context

Two kinds of copy debt, both hardware-found:

1. **Internal vocabulary leaking into product copy** on the crossover
   flow — `campaign/punchlist.md`'s #34 and the "residual
   ledger-jargon" item from the #1533 review (referenced in
   `campaign/VALIDATION-FIX-PLAN.md`'s Wave-3 W3.5 entry) named
   specific surviving strings.
2. **Run-18/21 hardware-pilot UX findings** (`campaign/punchlist.md`
   #46's "UX batch → W3" list) not yet folded into code — several are
   verified below against the current checkout.

**Line numbers below were re-verified against this kit's base SHA at
write time; `main` moves fast (see AGENTS.md "PR workflow on a
fast-moving main") — re-confirm every citation as a preflight fact
before editing, and if a string has moved or already been rewritten,
say so in the PR description rather than silently skipping it.**

## Required reading (in order)

Everything below is in this repository. (Context above also cites
Jasper's private campaign planning notes — punchlist item #34 and
item #46's run-18 UX batch — for provenance; those are **not** in this
repository and their substance is already quoted above.)

1. `docs/active-crossover-information-design.md` §"Language guide" —
   the canonical preferred/avoid vocabulary table. Every rewrite in
   this workstream should either use a term already in the "Prefer"
   list or extend it (adding a new preferred term to the guide itself,
   in the same PR, if you introduce one not already there — the guide
   drifting out of sync with actual product copy is exactly the kind
   of debt this workstream exists to prevent).
2. `jasper/active_speaker/crossover_envelope.py` — the four sites
   listed under Requirements below, read each in its surrounding
   ~15-line context before rewriting (some sit inside verdict text
   that other logic depends on for its *length* or *structure*, not
   just its words — confirm no test asserts on exact string equality
   you're about to break without updating).
3. `jasper/correction/level_match.py`'s `_RAMP_REFUSAL_PREFIX_COPY`
   table (~line 803) and `jasper/audio_measurement/ramp.py`'s
   `MAXED_OUT` terminal raise site (~line 1436) — the
   `safe_cap_below_window` duplication.

## Preflight facts (verify, then record base SHA)

- `jasper/active_speaker/crossover_envelope.py` contains the literal
  substring `"repeat ledger"` at (at time of writing) two verdict-text
  sites — search `git grep -n "repeat ledger"
  jasper/active_speaker/crossover_envelope.py` and use whatever it
  currently reports, not the numbers in this prompt, as ground truth.
- The same file contains `"Repeat safety state is unavailable"` in one
  nudge-text site — `git grep -n "Repeat safety state"`.
- `jasper/correction/level_match.py` maps the prefix `"safe cap
  reached below target window"` to user-facing text that includes
  *"Raise the external amplifier a little, then retry."* —
  `git grep -n "safe cap reached below target window"
  jasper/correction/level_match.py`.
- `jasper/audio_measurement/ramp.py` raises the raw error string
  `"safe cap reached below target window; raise the external amplifier
  and retry"` (the advice clause — *"raise the external amplifier and
  retry"* — duplicates the level_match.py mapped copy above almost
  verbatim) — `git grep -n "safe cap reached below target window"
  jasper/audio_measurement/ramp.py`.

## File allowlist

Modify:
- `jasper/active_speaker/crossover_envelope.py` (copy only — the four
  sites under Requirements)
- `jasper/audio_measurement/ramp.py` (the one raw-error string under
  Requirements — trim the advice clause, keep the fact)
- `jasper/correction/level_match.py` (only if the mapped copy itself
  also needs a wording pass — check for overlap with W3.4's Room audit
  first, per that prompt's coordination note)
- `docs/active-crossover-information-design.md` (Language guide table,
  additive)
- Existing crossover-envelope tests that assert on
  the strings you change (update the expected text)

Do NOT touch measurement logic, refusal-code identity (the machine
`code` field on any refusal — only its human-readable text/copy is in
scope), or any file outside this list. This is a pure copy-and-label
sweep.

## Requirements

### Crossover envelope copy (jargon)

1. Both `"...their exact repeat ledger is not complete..."` verdict
   strings — rewrite to name the user's actual situation without the
   internal term "ledger". These verdicts fire when a driver's
   near-field acoustics are saved but a repeat-completeness check
   isn't satisfied for the current topology/profile; state that in
   plain terms ("Run the driver measurement again to finish the
   repeat check" style — match the Language guide's imperative,
   task-first phrasing, e.g. "Measure {role} again").
2. `"Repeat safety state is unavailable. Run the microphone level
   check again before another sweep."` — same treatment; "repeat
   safety state" is internal-mechanism language for "JTS lost track of
   whether this measurement is still safely repeatable."

### `safe_cap_below_window` advice duplication

`ramp.py`'s raw error currently embeds advice
(*"...raise the external amplifier and retry"*) that
`level_match.py`'s mapped user-facing copy repeats independently
(*"Raise the external amplifier a little, then retry."*) — two
independently-worded copies of the same instruction, one of which
(`ramp.py`'s) is meant to be an internal fact string, not
user-facing copy (its consumers include log lines and the `error`
field other code branches on, not just the mapped-copy path). Trim
`ramp.py`'s raw string to the **fact only** (the cap was reached below
the target window — no imperative verb, no "raise the amplifier"
clause); keep `level_match.py`'s user-facing copy exactly as the one
place that gives advice. Confirm nothing else pattern-matches on the
advice clause specifically (the `_RAMP_REFUSAL_PREFIX_COPY` matching
is prefix-based on the *fact* clause, not the advice clause — check
this holds before trimming).

## Anti-overengineering fences

Do NOT: build a general-purpose label-sanitization or copy-templating
library for any of the above — each fix is a small, targeted change at
its named site; add a copy-lint/glossary-enforcement test suite beyond
extending the existing Language-guide table (a full automated jargon
linter is out of scope — this is a manual sweep); change the
underlying refusal `code` values (only the human-readable text);
persist calibration-mode choice beyond the single relay session's
lifetime (that's already `household_mic.json`'s job for cross-session
persistence — this fix is about within-session continuity only, don't
duplicate the cross-session mechanism).

## Tests

- One test per crossover-envelope copy rewrite, asserting the new
  string and the absence of the old jargon term (`assert "ledger" not
  in text`, `assert "repeat safety state" not in text.lower()`, or
  equivalent) — mirror however existing envelope-copy tests in
  `tests/test_active_speaker_crossover_envelope.py` (or wherever W3.3
  landed its tests) are structured.
- One test asserting `ramp.py`'s raw `MAXED_OUT` error string no
  longer contains the advice clause, and one confirming
  `level_match.py`'s mapped copy still contains it (the fact moved out
  of one layer, the advice stayed in the other — pin both sides so a
  future edit can't silently duplicate them again).

## Acceptance commands

```
.venv/bin/pytest tests/test_active_speaker_crossover_envelope.py \
  tests/test_audio_measurement_ramp.py tests/test_correction_level_match.py -q
scripts/test-fast
```

## Hardware validation (JTS3 + a real phone, UX rubric)

1. Reproduce a repeat-ledger verdict and a "Repeat safety state"
   nudge (or confirm current code paths to reach them, since Wave-2
   fixes narrowed when they fire) — confirm the new copy.
2. Reproduce (or confirm current unreachability of) a
   `safe_cap_below_window` refusal — confirm the fact/advice split
   reads correctly in both the log line and the household-facing
   text.
3. Apply the UX rubric to every screen touched, with particular
   attention to point 3 (concise) and point 4 (value not jargon).
