# Cutover map — `jasper/active_speaker/crossover_v2_flow.py`

> **This map dies when tier 7 completes.** It is scaffolding for the
> dissolution of one file, not a durable description of the system. When the
> last concern below has left `crossover_v2_flow.py`, delete this document and
> its `docs/doc-map.toml` row. Nothing should ever cite it as an authority for
> how the tuning flow works — [docs/crossover-v2-engine-design.md](crossover-v2-engine-design.md)
> owns that, and [docs/REFACTOR-CUTOVER-2026-08.md](REFACTOR-CUTOVER-2026-08.md)
> owns the plan this map serves.

**STATUS:** in progress — line ranges derived at HEAD `c253c3cf1`.

Scope: `jasper/active_speaker/crossover_v2_flow.py` only. The sibling map for
`jasper/web/correction_crossover_v2.py` is a separate document.

## Sections

1. The concern table
2. Deletion order
3. Load-bearing oddities
4. Size accounting
