# Third-party attribution — firmware

Vendored / third-party source and asset files checked into `firmware/`,
with their upstream and the actual license status as best determined.

This is the firmware-scoped companion to the repo-root
[`LICENSE-third-party.md`](../LICENSE-third-party.md); the rows here
expand the firmware entries in that inventory with the upstream-license
lookups that were previously marked "needs verification".

Last reviewed: 2026-06-09.

> **Redistribution is NOT cleared.** Every vendored component below is
> either explicitly unlicensed upstream or carries unresolved
> generated-output terms. Do not treat any of these files as cleared
> for redistribution in a public firmware image until the gaps in the
> "License/status" column are closed — by obtaining written permission,
> finding a stated upstream license, or replacing the file with a
> known-permissive equivalent.

## Vendored components

| Component | Files | Upstream | License/status |
|---|---|---|---|
| ELECROW CrowPanel CST816D touch driver | `dial/src/CST816D.h`, `dial/src/CST816D.cpp` | <https://github.com/Elecrow-RD/CrowPanel-1.28inch-HMI-ESP32-Rotary-Display-240-240-IPS-Round-Touch-Knob-Screen> (`/factory_soucecode/RotaryScreen_1_28/`) | **No license declared upstream.** Verified 2026-06-09: the Elecrow repository has no `LICENSE`, `COPYING`, or `NOTICE` file and states no license anywhere. Under default copyright that makes it all-rights-reserved. **NOT cleared for redistribution.** No SPDX id has been added to these files because none is known. |
| Dial factory volume-gauge UI image data | `dial/src/assets/ui_img_v2_bj_volume_100_png.c`, `dial/src/assets/ui_img_icon_volume_40_png.c`, `dial/src/assets/ui_img_bar_light_01_png.c`, `dial/src/assets/ui_img_bar_bule_02_png.c` | Image data generated by SquareLine Studio 1.5.0 (LVGL 8.3.6) from ELECROW's factory artwork in the repo above (`/example/libraries/UI/ui_img_*.c`) | **Indeterminate — two layers.** (1) SquareLine Studio's terms for *redistributing generated output* are not publicly stated in its docs/license pages (verified 2026-06-09); the EULA is presented in-app only. (2) Even if the tool grants the user full rights to their own export, the *source artwork* here is ELECROW's factory art from the unlicensed repo above, so the underlying images are all-rights-reserved regardless. **NOT cleared for redistribution.** No SPDX id added (none known); the generated data is untouched. |

## Notes on adjacent (NOT vendored) files

These cite the ELECROW factory source for hardware facts but are
JTS-original work, so they are not listed as third-party above:

- `dial/src/assets/factory_volume.h` — JTS-authored glue header that
  `LV_IMG_DECLARE`s the four vendored image symbols and documents the
  vendoring. It contains no upstream code or data.
- `dial/include/config.h` — JTS-authored pin/layout constants. The pin
  numbers are taken from the CrowPanel factory firmware (cited inline);
  pin mappings are hardware facts, not copyrightable expression.
- `dial/src/display.cpp`, `dial/src/scenes.cpp`, `dial/src/main.cpp`,
  `dial/include/lv_conf.h`, `dial/platformio.ini`, `dial/build.sh` —
  JTS-original firmware that references the CrowPanel/LVGL upstreams in
  comments but copies no upstream source.

Build-time library dependencies pulled by each `platformio.ini` (LVGL,
LovyanGFX, FastLED, ArduinoJson, Improv Wi-Fi, Arduino_GFX, the
Arduino-ESP32 / pioarduino toolchain) are tracked in the repo-root
[`LICENSE-third-party.md`](../LICENSE-third-party.md), not here — those
are fetched at build time, not vendored into `firmware/`.

## Follow-up

- ELECROW CST816D driver and the four UI image blobs: obtain written
  redistribution permission from ELECROW, or replace the driver with a
  permissively licensed CST816-family driver and regenerate the volume
  gauge from owned/permissive artwork, before shipping a public
  firmware image that contains them.
- If ELECROW later adds an explicit license upstream, record it here and
  add the matching `SPDX-License-Identifier` header to the vendored
  source files.
