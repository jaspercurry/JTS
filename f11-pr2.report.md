Astra (GPT-6 / Codex)

Built F11 PR 2 locally on `codex/f11-pr2`. Completion is blocked by the three items below. No network, push, deploy, SSH, or main-checkout edit was used.

- `_measured_level_trims` cannot be deleted while preserving the required `_compare_level_sittings`: that function calls it. The public `measured_level_trims` wrapper also has live readers in `jasper/cli/measure.py`, `jasper/cli/null_door.py`, and `jasper/web/correction_crossover_v2_state.py`. I kept this shared reader and its needed helpers. `_derive_corrections` and automatic setup review are deleted. Replacing these remaining measurement readers needs a separate brief.
- The read-only `tests/test_ring_active_endpoint.py` still imports the deleted builder at lines 1914 and 2064. `test_ring_candidate_refuses_a_typod_wire_as_a_typed_config_error` and `test_every_emit_devices_field_reaches_the_emitter` fail with `ImportError`; the latter also lists both old builder emit paths at lines 2324 and 2329. These pins cannot pass after deleting the builder without changing their fixture/guard coverage. I left the whole file unchanged, as instructed.

- The full fast lane cannot pass on this host: socket binds fail with `PermissionError`, render children fail while applying resource bounds, and five existing filesystem checks report group-write/setgid differences. The failed run's counts and the subsequent source fixes are below. No host-limit guard or test was weakened.

Commit `ff55c7d4c` contains the first deletion batch. The second cleanup commit contains the caller, test, ADR, and report changes. Both have the Astra trailer. Some combined shell invocations failed on the sandbox index lock; separate `git add` and `git commit` calls succeeded.

Deleted the second builder, both unused apply functions, the ADR-0195 comparison helpers and unused `revalidation.py`. The remaining callers use the existing composer path; automatic review is retired. Grouping uses one relocation path with an applied or declared candidate. Channel choice, pair trim, applied layers, and refusal behavior have passing behavior tests. The summed-test handler uses `load_commissioning_view`.

Added [ADR-0312](docs/adr/0312-the-ladder-has-no-rebuild-to-compare.md), dated 2026-09-13, Accepted, superseding ADR-0195. The reserved number was free.

Judgment: a saved applied record carries the earlier driver proof until playback is shown to have moved; a changed candidate identity only adds the stale disclosure. Composer readiness still controls the review and save action.

| File | Before | After |
|---|---:|---:|
| `jasper/active_speaker/baseline_profile.py` | 3228 | 1850 |
| `jasper/active_speaker/setup_status.py` | 882 | 792 |
| `jasper/active_speaker/commissioning_coordinator.py` | 1057 | 970 |
| `jasper/multiroom/active_profile.py` | 116 | 104 |
| `jasper/web/sound_active_speaker.py` | 3791 | 3772 |

Setup key reader check covered `jasper/web`, `jasper/cli/doctor`, `jasper/control`, and all JavaScript below `deploy/assets`:

| Key | Readers found and action |
|---|---|
| `provisional` | `state_aggregate._active_speaker_level_match_provisional` now reads the applied `protected_profile`; keep that field. `sound-profile/js/active-speaker-ui.js::levelMatchSummary` reads the raw baseline field; keep the baseline key too. |
| `revalidation` | `sound-profile/js/main.js::baselineProfileRevalidation` and combined-test rendering read the raw baseline object. Keep the setup key as `{required: false, status: "not_required"}`. No second comparison remains. |
| `recomposition_snapshot` / `recomposition_snapshot_available` | No setup-summary consumer. The first was an internal read used to make the second. Both are removed from setup derivation. Applied records retain their snapshot for graph readers. |
| `linearization_outcome` | No setup-summary consumer. The match in `web/correction_crossover_v2.py` is a docstring about analysis output. Removed from `protected_profile`. |
| `automatic_candidate` | No reader in the searched surfaces. Removed from both setup summary levels. The applied record's bank metadata remains because `_bank_applied_base_trim` consumes it. |
| `status`, `path`, `config_path`, `source_fingerprint`, `candidate_fingerprint`, `issues` | Kept. Control consumes the setup result; doctor reads `protected_profile.layer_a_binding` and setup `issues`. Raw web baseline readers also use status, config, and identity. No additional consumer of setup `path` or `source_fingerprint` was found. |

Other product files touched: `jasper/active_speaker/{bundles,crossover_v2_flow,measured_crossover_candidate}.py`, `jasper/cli/{basic_profile,doctor/active_speaker}.py`, `jasper/control/state_aggregate.py`, `jasper/multiroom/active_leader_config.py`, and `jasper/web/correction_crossover_v2_state.py`. Most changes remove stale prose; control now reads saved provenance from the applied summary. Deleted `jasper/active_speaker/revalidation.py`.

Test files touched: `tests/active_speaker_fixtures.py`, plus `test_active_speaker_{baseline_profile,commissioning_coordinator,local_subwoofer,setup_status}.py`, `test_control_state_aggregate.py`, `test_multiroom_{active_leader,follower}_config.py`, `test_plan_run.py`, `test_reconcile_{adoption_durability,camilla_down}.py`, `test_sound_graph_carrier.py`, and `tests/crossover_v2_round_harness.py`. Shared old fixtures remain in the smaller baseline test file; new declared graph fixtures live in the shared fixture module. The round harness now imports the preset compiler from its `staging` owner. `docs/doc-map.toml` classifies the required root report as a session artifact; its read-only orphan-doc guard is unchanged. No fixture was copied. Tests for the removed builder/comparison/automatic derivation were deleted; shared bank and graph tests remain.

Checks used the required prefix: `PYTHONPATH=$PWD /Users/jaspercurry/Code/JTS/.venv/bin/pytest <targets> -q`.

- Main verification: **900 passed, 1 skipped**. Targets: `test_transport_endpoint_preservation.py`, `test_canonical_target_registration.py`, `test_active_speaker_emit_gate.py`, `test_cli_seat_level.py`, `test_active_speaker_setup_status.py`, `test_active_speaker_commissioning_coordinator.py`, `test_multiroom_*.py`, `test_active_speaker_baseline_profile.py`, `test_reconcile_adoption_durability.py`, `test_reconcile_camilla_down.py`, `test_sound_graph_carrier.py`, `test_control_state_aggregate.py`, and `test_sound_setup_summed_test_active.py`.
- Final commissioning view check: **45 passed**. Final baseline fixture check: **22 passed**.
- Additional read-only guards: **42 passed** across `test_tuning_tool_menu_generator.py`, `test_log_event_conventions.py`, `test_logging_setup.py`, and `test_measurement_mover_agnostic.py`.
- The first wider touched-test run had **15 failed, 1149 passed**. All failures outside the read-only ring guard were fixed and checked again. That run also covered `test_active_speaker_local_subwoofer.py`, `test_plan_run.py`, `test_active_speaker_web_commissioning.py`, `test_correction_crossover_v2_endpoints.py`, `test_active_speaker_level_match.py`, and `test_active_speaker_protection_floor.py`.
- Ring plus required guard run: **2 failed, 253 passed, 1 skipped**, with the two blocked ring pins listed above.
- `/Users/jaspercurry/Code/JTS/.venv/bin/ruff check jasper tests` and final `ruff check .`: passed after removing unused fixture data and imports.
- Exact requested mypy command, `git diff --name-only origin/main...HEAD -- 'jasper/*.py' | xargs env PYTHONPATH=$PWD /Users/jaspercurry/Code/JTS/.venv/bin/mypy`: ran after both commits and **failed** because the diff includes deleted `jasper/active_speaker/revalidation.py`. Then ran `git diff --name-only --diff-filter=ACM origin/main...HEAD -- 'jasper/*.py' | xargs env PYTHONPATH=$PWD /Users/jaspercurry/Code/JTS/.venv/bin/mypy`: **passed, all 13 remaining changed Python files**. No type error remains in a changed source file.
- Exact fast lane: `set -o pipefail; PYTEST=/Users/jaspercurry/Code/JTS/.venv/bin/pytest RUFF=/Users/jaspercurry/Code/JTS/.venv/bin/ruff PYTHONPATH=$PWD bash scripts/test-fast`. The first run failed on the fixed unused variable. Final sentinel: **`==> test-fast: FAILED`**. Pytest reported **77 failed, 14910 passed, 80 skipped, 251 errors**. Thirty-two failures came from the round harness reading a removed incidental import; one came from the new root report lacking doc-map registration. Both source issues are fixed: `test_crossover_v2_round_wiring.py` plus `test_docs_impact.py` now give **92 passed**. The full lane was not repeated after these focused fixes because its known host and read-only ring blockers remain. The other failures were the two ring pins, 34 socket tests, three render tests, and five filesystem tests. Errors were 239 socket setups and 12 render setups. Socket traces stop at `socket.bind` with errno 1. Render traces stop in `bench/render.py` with a failed child `preexec_fn`. Filesystem cases are `test_doctor_audio_runtime_camilla.py::test_camilla_configs_writable_verdicts[group-writable]`, two cases in `test_doctor_correction.py`, and the two happy-path cases in `test_doctor_secrets.py` (expected mode 2770, observed 0770).
- `git diff --check`: passed. `git diff --stat origin/main -- jasper/camilla.py jasper/active_speaker/measurement_emit.py`: no output, both unchanged. The named repo-wide guard files are unchanged too.

No listed CLI verb or flag changed; menu generation was not required, and its guard passed. The built-in `/code-review` command is not available in this offline tool surface; I did a local sanity review of the diff. No merge or hardware checks were run because this brief forbids network and deployment.
