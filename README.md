# Reference only — never merge

Scratch analysis scripts, tune documents and the lab book (`5405-twomic/search/LOG.md`) from the jts3 cardioid session of 2026-09-19/20 (issues #5405, #5438, #5439). This is an ORPHAN branch: it shares no history with `main` and must not be merged. The code is informal, reaches into product internals at build `c6095d21d`, and includes dead ends. Use it as a reference when porting the pieces listed in #5439 into the product with proper tests. No recordings and no calibration files are included.
