# deploy/constraints-pi.txt — Pi-generated pip constraints.
#
# GENERATED FILE — do not hand-edit version pins. Regenerate with:
#   bash scripts/generate-pi-constraints.sh
# then review the diff and commit (see the script header for the
# full generate -> review -> commit workflow).
#
# One narrow exception, for the cross-ecosystem pin chain only (#1275):
#   python3 scripts/align-pi-constraint-pins.py
# rewrites just those pins to match uv.lock, which is what a uv-side
# dependency bump needs to land. Everything else still comes from a Pi.
#
# Provenance:
#   generated-at: 2026-06-19T18:51:37Z
#   source-pi:    pi@192.168.1.53
#   pi-python:    Python 3.13.5
#   pi-pip:       pip 26.1.2 from /opt/jasper/.venv/lib/python3.13/site-packages/pip (python 3.13)
#   pi-build:     JASPER_GIT_SHA=7bf41dac-dirty JASPER_GIT_SHA_FULL=7bf41dac8b9097aa977ba46ab89194df62e53e85-dirty JASPER_GIT_BRANCH=HEAD JASPER_INSTALL_AT=2026-06-19T14:50:08-04:00
#
# install.sh passes this file to pip via -c ONLY when it exists;
# deleting it reverts deploys to open-range resolution.
#
# 2026-07-11 coherence update: the protobuf chain was deliberately
# co-resolved in pyproject.toml/uv.lock and this Pi overlay. JTS replaced
# nyct-gtfs (which hard-pinned the unsupported protobuf 4.25.3 runtime)
# with the standard GTFS-Realtime bindings, then moved protobuf and every
# affected consumer together. tests/test_constraints_pi_resolvable.py
# guards the chain in both ecosystems. A proper on-Pi regeneration after
# the next deploy overwrites this note and refreshes the provenance above.
#
# 2026-08-08 HAND-EDIT (not a Pi regeneration): raised exactly three
# security pins to match the co-resolved uv.lock, without re-running
# scripts/generate-pi-constraints.sh — cryptography 49.0.0 -> 50.0.0
# (CVE-2026-69247), httplib2 0.31.2 -> 0.32.0 (CVE-2026-59939), pyasn1
# 0.6.3 -> 0.6.4 (CVE-2026-59884/59885/59886, the latter two forced via
# the new [tool.uv].constraint-dependencies floor since pyasn1 is
# transitive-only here). No other line in this file was touched. A
# proper on-Pi regeneration overwrites this note and refreshes the
# provenance above.
annotated-types==0.8.0
anyio==4.14.2
audioop-lts==0.2.2
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
cryptography==50.0.1
dbus-next==0.2.3
distro==1.9.0
evdev==1.9.3
flatbuffers==25.12.19
google-api-core==2.34.0
google-api-python-client==2.199.0
google-auth-httplib2==0.4.2
google-auth-oauthlib==1.4.1
google-auth==2.53.0
google-genai==2.9.0
googleapis-common-protos==1.75.2
gtfs-realtime-bindings==2.2.0
h11==0.16.0
httpcore==1.0.9
httplib2==0.32.0
httpx==0.28.1
idna==3.19
ifaddr==0.2.0
importlib_resources==7.1.0
jiter==0.16.0
joblib==1.5.3
libusb-package==1.0.30.0
numpy==2.4.6
oauthlib==3.3.1
onnxruntime==1.27.0
openai==2.41.1
openwakeword==0.6.0
packaging==26.3
proto-plus==1.28.4
protobuf==7.36.0
pyalsaaudio==0.11.0
pyasn1==0.6.4
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
Pygments==2.21.0
pyparsing==3.3.2
pyudev==0.24.4
pyusb==1.3.1
PyYAML==6.0.3
RapidFuzz==3.14.5
redis==8.1.0
requests-oauthlib==2.0.0
requests==2.34.2
scikit-learn==1.9.0
scipy==1.17.1
sdnotify==0.3.2
sniffio==1.3.1
sounddevice==0.5.6
spotipy==2.26.0
tenacity==9.1.4
threadpoolctl==3.6.0
tqdm==4.70.0
typing-inspection==0.4.4
typing_extensions==4.16.0
uritemplate==4.2.0
urllib3==2.7.0
websocket-client==1.9.1
websockets==16.0
wheel==0.47.0
zeroconf==0.149.16
