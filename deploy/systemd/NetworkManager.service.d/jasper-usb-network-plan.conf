[Unit]
# Give the oneshot the first chance to render both USB projections before NM.
# Wi-Fi is the recovery path and still starts if the plan fails; the gadget's
# hard dependency prevents it from creating usb0 against an incoherent pair.
Wants=jasper-usb-network-plan.service
After=jasper-usb-network-plan.service
